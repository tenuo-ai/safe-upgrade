node_modules/
dist/
artifacts/
coverage/
*.tsbuildinfo
.env
.env.*
# The template carries variable names only, never values.
!.env.example
.DS_Store

# Engineering notes. Local only — not the user guide.
docs/
internal/
